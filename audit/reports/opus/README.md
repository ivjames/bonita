# Audit results — fixture self-test run

These reports were generated by `tools/audit.mjs` against the **local test
fixtures** (`tools/fixtures/`, served at `http://127.0.0.1:8199/`), not the live
BCA website. The fixtures contain deliberately planted accessibility and content
defects, so these numbers exercise and verify the tool — they are **not** a real
audit of bonitacenterforthearts.com.

They are committed here as a checked-in sample of the tool's output shape.
A real pre-migration audit requires running the crawler from an environment with
outbound web access:

```bash
cd tools && npm install
node audit.mjs https://www.bonitacenterforthearts.com --max 40
```

## Files

| File | Contents |
|------|----------|
| `summary.md` | Top-level counts (pages, images, violations, errors) |
| `inventory.md` / `inventory.json` | Per-page headings, images+alt, links, embeds, PDFs, forms |
| `a11y.md` / `a11y.json` | axe-core WCAG 2.1 A/AA scan, aggregated by rule + per-page detail |

## This fixture run

- 3 pages crawled (`/`, `/about.html`, `/index.html`)
- 11 images, 7 flagged for missing/auto-generated alt text
- 39 axe violation instances across 8 distinct rules
- 0 crawl errors
